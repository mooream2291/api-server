//the using-mongoose.js file is where our server lives (we can name this file whatever we want)

//1. build a giant server file and then decide how to modularize it, make sure everythign is connected first

//What does out dotenv do? 

    //this is a file that creates new env variables on our machine so that we can access them elsewhere?

//class notes

//LINKED LIST

class Node {
    constructor(value) {
        this.value = value;
        this.next = null;
    }
}

module.exports = Node;